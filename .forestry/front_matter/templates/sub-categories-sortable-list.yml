---
label: Sub-Categories (sortable list)
hide_body: false
is_partial: true
fields:
- type: list
  name: states
  label: States
  config:
    use_select: true
    source:
      type: pages
      section: states
- type: list
  name: sectors
  label: Sectors
  config:
    use_select: true
    source:
      type: pages
      section: sectors
- type: list
  name: subsectors
  label: Subsectors
  config:
    use_select: true
    source:
      type: pages
      section: subsectors
